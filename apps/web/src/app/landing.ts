/**
 * Kept as a re-export so existing imports keep working.
 *
 * The answer now lives in `@medsupply/navigation`, beside the rest of the
 * navigation policy, because mobile needs it too and had been keeping its own
 * copy of the same matrix.
 */
export { LANDING_ROUTE, landingRouteFor } from '@medsupply/navigation';
